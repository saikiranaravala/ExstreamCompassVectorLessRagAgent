# Minimal dependencies for the active demo path on Render free tier.
# The full requirements.txt is for local dev / Docker (includes agent, indexer,
# observability stacks that are not wired in the demo path).

# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Auth & security
authlib>=1.3.0
PyJWT>=2.8.0

# Data & config
pydantic>=2.6.0
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# HTTP
httpx>=0.27.0
